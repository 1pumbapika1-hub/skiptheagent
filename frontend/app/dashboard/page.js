export default function Dashboard() {
  return (
    <main className="onboarding-page">
      <section className="onboarding-hero">
        <p className="eyebrow">Dashboard</p>
        <h1>Your seller dashboard is coming together.</h1>
        <p>
          This will show your listings, enquiries, open homes, solicitor requests,
          and payment status.
        </p>
      </section>
    </main>
  );
}
